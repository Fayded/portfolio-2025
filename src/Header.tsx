import Logo from './Logo';

function Header() {
  return (
    <header className="container-fluid">
      <div className="grid">
        <Logo />
      </div>
    </header>
  );
}

export default Header;
